import React, { FC } from 'react'
import { DetailProvider } from '@/contexts/detail'

const DetailTemplate: FC = () => {
  return <DetailProvider>aaa</DetailProvider>
}

export default DetailTemplate
